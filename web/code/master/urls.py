from django.conf.urls import url, include
from master import views

urlpatterns = (
    # url(r'^googleproductcategory', views.GoogleProductCategory_List, name='productdata_googleproductcategory_list'),
    # url(r'^$', views.Global_Home, name='global_home'),
)
